<%= turbo_frame_tag "content" do %>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="px-4 py-5 sm:px-6">
      <div class="tinymce-content">
        <%= raw(I18n.locale == :en ? @page.english_content : @page.content) %>
      </div>
    </div>
  </div>
<% end %>
